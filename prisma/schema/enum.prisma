enum Role {
  USER
  ADMIN
  SUPER_ADMIN
}

enum UserStatus {
  ACTIVE
  INACTIVE
  SUSPENDED
  PENDING_VERIFICATION
}

enum TokenType {
  PASSWORD_RESET
  EMAIL_VERIFICATION
  OTP
  REFRESH_TOKEN
}

enum ProjectStatus {
  ACTIVE
  INACTIVE
  SUSPENDED
}

enum ConversationType {
  DIRECT
  GROUP
}

enum ParticipantRole {
  MEMBER
  ADMIN
}

enum MessageType {
  TEXT
  IMAGE
  VIDEO
  AUDIO
  FILE
  SYSTEM
}

enum RequestStatus {
  PENDING
  ACCEPTED
  REJECTED
}
